function minhaFuncao(param) {
  // bloco de código
}
minhaFuncao("param");

// Expressão de função

const soma = function(num1, num2) {return num1 + num2}
console.log(soma(2, 3));

// Diferença principal, 

console.log(apresentar());

// Esta forma a função pode ser chamada antes de ser declarada

function apresentar() {
  return 'Olá';
}